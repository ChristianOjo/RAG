export default function Home() {
  return (
    <div style={{ padding: '50px', textAlign: 'center' }}>
      <h1>RAG Chatbot Frontend</h1>
      <p>The frontend is now deployed! The backend API is at /api/query.</p>
      <p>You need to build the full UI here to call the API.</p>
    </div>
  );
}
